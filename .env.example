# ============================================================
# .env.example — MẪU (KHÔNG chứa key thật). Copy thành .env rồi
# điền giá trị thật. .env đã gitignore, .env.example được commit.
# ============================================================
# API key 9router (proxy .227:20128) — dùng cho skill vision-analyzer
NINE_ROUTER_KEY=
